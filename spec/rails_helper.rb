require 'spec_helper'

# Regenerate the dummy app according to the version of Rails we have

# Run te same generatore a `rail plugin new` uses

# Add the route and Home Controller

# Run the install generator

require File.expand_path('../dummy/config/environment.rb', __FILE__)

